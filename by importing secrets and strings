import  secrets
import string
alphabets = string.digits+string.ascii_uppercase+string.ascii_lowercase+string.punctuation
password = "".join(secrets.choice(alphabets)for i in range(int(input("Enter the range: "))))
print(password)
